# rhys.streefland.co.uk

Personal website built with Hugo.

## Adding a Blog Post

1. Create new post: `hugo new posts/my-post.md`
2. Edit `content/posts/my-post.md` and set `draft: false`
3. Build: `hugo --minify`
4. Commit and push both `content/` and `docs/` folders
